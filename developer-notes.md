```pwsh
# Generate Web API Boilerplate from template
dotnet new webapi

# Generate .gitignore file (and edit file to include .env files)
dotnet new gitignore

# Install MongoDB drivers
dotnet add package MongoDb.Driver
```